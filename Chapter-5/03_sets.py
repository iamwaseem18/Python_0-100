s = set() #Empty set can be written like this

# The values in the set are counted only showed only once in the
# Output even if the value is repeated multiple times

z = {5,5,5,5, 6, "Waseem"}
print(z)
# o/p = {5, 6, 'Waseem'}

# The order in the o/p cant be defined

# Properties in sets

# 1) Sets are unordered
# 2) Sets are unindexed
# 3) There is no way to change items in sets'
# 4) Sets cannot contain duplicate values.